<main id="main">
	<div class="main_done">
		<div class="main_done-container">
			<h2 class="main_done_title">
				<% if (locals.result.studentRank <= 5) { %>
					최고의 플레이
				<% } else { %>
					탁월합니다
				<% } %>
			</h2>
			<p class="main_done_message">
				<%= locals.studentID.name %>님께서
				<% if (typeof locals.result.prevRankStudent != 'undefined') { %>
					<%= locals.result.prevRankStudent %>님의 기록을 제치고
				<% } %>
				랭킹 <%= locals.result.studentRank %>위에 등극하셨습니다.
				<br>
				방금 전 문장을 외워오시면 고급과자를 드립니다.
			</p>
			<input type="submit" id="main_login_input-end" class="typebattle_input-button main_done_input-submit" value="확인" autofocus="true">
		</div>
	</div>
</main>